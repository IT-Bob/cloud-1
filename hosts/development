# development
all:
  children:
    dev:
      children:
        vagrant:
          hosts:
            alpine_vagrant:
              ansible_port: 4345
              vagrant_hostname: cloud1-alpine
            debian_vagrant:
              ansible_port: 3345
              vagrant_hostname: cloud1-debian
            fedora_vagrant:
              ansible_port: 2345
              vagrant_hostname: cloud1-fedora
            ubuntu_vagrant:
              ansible_port: 1345
              vagrant_hostname: cloud1-ubuntu
          vars:
            ansible_user: vagrant
            ansible_host: 127.0.0.1
            ansible_ssh_private_key_file: .vagrant/machines/{{ vagrant_hostname }}/virtualbox/private_key
        virtualbox:
          hosts:
            ubuntu_vb:
              ansible_port: 32022
            fedora_vb:
              ansible_port: 34022
            manjaro_vb:
              ansible_port: 35022
          vars:
            ansible_host: 127.0.0.1
            ansible_user: aguerin
            ansible_ssh_private_key_file: ./keys/id_rsa
    # Groups aliases
    vb:
      children:
        virtualbox:
